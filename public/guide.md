# 🚀 ZTH Coder (KMN Prompt Generator) - AI Prompt Guide

> ဒီ Website ကြီးတစ်ခုလုံးကို Lovable AI ကို Prompt ရေးပြီး တည်ဆောက်ထားတာဖြစ်ပါတယ်။
> အောက်မှာ အစကနေ အဆုံးထိ ဘယ်လို Prompt တွေနဲ့ လုပ်ခိုင်းခဲ့တယ်ဆိုတာ အဆင့်ဆင့် ဖော်ပြထားပါတယ်။

---

## 📋 မာတိကာ

1. [Project စတင်ခြင်း](#1-project-စတင်ခြင်း)
2. [Authentication System](#2-authentication-system)
3. [Prompt Generator Core Feature](#3-prompt-generator-core-feature)
4. [Coding Prompt Tab](#4-coding-prompt-tab)
5. [Image Prompt Tab](#5-image-prompt-tab)
6. [Video Prompt Tab](#6-video-prompt-tab)
7. [Image Generator Tab](#7-image-generator-tab)
8. [Ad Poster Generator Tab](#8-ad-poster-generator-tab)
9. [Image to Prompt Tab](#9-image-to-prompt-tab)
10. [Execute (AI Answer) Feature](#10-execute-ai-answer-feature)
11. [UI/UX Improvements](#11-uiux-improvements)
12. [How To Use Guide](#12-how-to-use-guide)
13. [Admin Panel](#13-admin-panel)
14. [SEO & Branding](#14-seo--branding)
15. [🔗 API Integration & Edge Functions](#15--api-integration--edge-functions)
16. [💻 Coding Prompt - Use Case Guide](#16--coding-prompt---use-case-guide)

---

## 16. 💻 Coding Prompt - Use Case Guide

> ဒီ section မှာ **Coding Prompt Generator** ထဲက Use Case တစ်ခုချင်းစီကို ဘယ်အချိန်မှာ ဘယ်လိုသုံးရမလဲဆိုတာ အသေးစိပ်ရှင်းပြထားပါတယ်။

### 📋 Use Cases Overview

| Use Case | ဘယ်အချိန်သုံးမလဲ | Example Scenario |
|----------|------------------|------------------|
| 🚀 Feature တည်ဆောက် | Feature အသစ်ရေးမယ် | Login system, Dashboard, Payment |
| 🐛 Debug / Fix | Bug ရှာပြီးပြင်မယ် | Error ဖြေရှင်း, Crash fix |
| ♻️ Refactor | Code ပိုကောင်းအောင်ပြင်မယ် | Code cleanup, Performance improve |
| 🔗 API Integration | External API ချိတ်မယ် | Stripe, Google Maps, OpenAI |
| 🗄️ Database Design | DB structure ရေးမယ် | Schema design, Complex queries |
| 🔐 Authentication | Login/Auth system | JWT, OAuth, Role-based access |
| 🧪 Testing | Test ရေးမယ် | Unit test, Integration test |
| 📦 Deployment | Deploy/DevOps | Docker, CI/CD, Cloud setup |
| ⚡ Optimization | Performance ပိုကောင်းအောင် | Speed up, Memory optimize |
| 🏗️ Architecture | System design | Microservices, Monolith structure |
| 🎨 UI Component | UI/UX component | Button, Modal, Form components |
| 🧮 Algorithm | Algorithm ရေးမယ် | Sorting, Searching, Data structures |

---

### 🚀 Feature တည်ဆောက် (Build Feature)

**ဘယ်အချိန်သုံးမလဲ?**
- Application မှာ **feature အသစ်** ထည့်ချင်တဲ့အခါ
- **New module** တစ်ခုလုံး ရေးချင်တဲ့အခါ
- **MVP (Minimum Viable Product)** တည်ဆောက်ချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "User registration and login system ရေးမယ်"
✅ "E-commerce shopping cart feature ထည့်မယ်"
✅ "Real-time chat feature ရေးမယ်"
✅ "Dashboard with charts and analytics ရေးမယ်"
✅ "File upload system ရေးမယ်"
```

**Output ရမှာ:**
- Complete feature implementation
- File structure recommendations
- All necessary components/modules
- Error handling included

---

### 🐛 Debug / Fix

**ဘယ်အချိန်သုံးမလဲ?**
- **Error/Bug** ရှာဖို့ လိုတဲ့အခါ
- Code ကို **run လို့မရ**တဲ့အခါ
- **Unexpected behavior** ဖြစ်နေတဲ့အခါ
- **Error message** ကို နားမလည်တဲ့အခါ

**Example Scenarios:**
```
✅ "TypeError: Cannot read property 'x' of undefined ဆိုပြီး error တက်နေတယ်"
✅ "API call က 500 error ပြန်နေတယ်"
✅ "State update လုပ်ပေမယ့် UI မ rerender ဘူး"
✅ "Login ပြီးတဲ့အခါ redirect မလုပ်ဘူး"
✅ "Memory leak ဖြစ်နေတယ်"
```

**Output ရမှာ:**
- Error ဖြစ်ရတဲ့ အကြောင်းရင်း analysis
- Step-by-step debugging guide
- Fix solution with code
- Prevention tips

---

### ♻️ Refactor

**ဘယ်အချိန်သုံးမလဲ?**
- Code က **ရှုပ်ပြီး ဖတ်ရခက်**တဲ့အခါ
- **Duplicate code** တွေ ရှိနေတဲ့အခါ
- **Performance** ပိုကောင်းအောင် လုပ်ချင်တဲ့အခါ
- **Best practices** နဲ့ align ဖြစ်အောင် ပြင်ချင်တဲ့အခါ
- Code ကို **maintainable** ဖြစ်အောင် လုပ်ချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "ဒီ function ကို ပိုရှင်းအောင် refactor လုပ်ချင်တယ်"
✅ "Callback hell ကို async/await နဲ့ ပြောင်းချင်တယ်"
✅ "Class component ကို functional component ပြောင်းချင်တယ်"
✅ "SOLID principles apply လုပ်ချင်တယ်"
✅ "Code ကို modular ဖြစ်အောင် ခွဲချင်တယ်"
```

**Output ရမှာ:**
- Before/After comparison
- Explanation of improvements
- Clean, readable code
- Best practices applied

---

### 🔗 API Integration

**ဘယ်အချိန်သုံးမလဲ?**
- **External API** (Third-party services) ချိတ်ချင်တဲ့အခါ
- **REST/GraphQL API** consume လုပ်ချင်တဲ့အခါ
- **Webhook** handler ရေးချင်တဲ့အခါ
- API **authentication** (API keys, OAuth) setup လုပ်ချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "Stripe payment gateway integrate လုပ်မယ်"
✅ "Google Maps API ချိတ်ပြီး location picker ထည့်မယ်"
✅ "OpenAI API နဲ့ chatbot ရေးမယ်"
✅ "Twilio SMS API integrate လုပ်မယ်"
✅ "Firebase push notification integrate လုပ်မယ်"
```

**Output ရမှာ:**
- API setup guide
- Authentication handling
- Request/Response handling
- Error handling for API calls
- Rate limiting considerations

---

### 🗄️ Database Design

**ဘယ်အချိန်သုံးမလဲ?**
- **Database schema** အသစ်ဖန်တီးချင်တဲ့အခါ
- **Complex SQL queries** ရေးချင်တဲ့အခါ
- **Data modeling** လုပ်ချင်တဲ့အခါ
- **Database optimization** လုပ်ချင်တဲ့အခါ
- **Migrations** ရေးချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "E-commerce database schema design လုပ်မယ်"
✅ "User, Posts, Comments relationships ရေးမယ်"
✅ "Complex reporting query ရေးမယ်"
✅ "Database indexing strategy ချမှတ်မယ်"
✅ "Many-to-many relationship ရေးမယ်"
```

**Output ရမှာ:**
- Schema design with diagrams
- SQL create statements
- Relationships explanation
- Indexes recommendations
- Sample queries

---

### 🔐 Authentication

**ဘယ်အချိန်သုံးမလဲ?**
- **Login/Signup system** ရေးချင်တဲ့အခါ
- **JWT/Session authentication** implement လုပ်ချင်တဲ့အခါ
- **OAuth** (Google, Facebook login) ထည့်ချင်တဲ့အခါ
- **Role-based access control (RBAC)** ရေးချင်တဲ့အခါ
- **Password reset/Email verification** ထည့်ချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "JWT authentication with refresh tokens ရေးမယ်"
✅ "Google OAuth login implement လုပ်မယ်"
✅ "Admin/User role-based access control ရေးမယ်"
✅ "Password reset via email ရေးမယ်"
✅ "Two-factor authentication (2FA) ထည့်မယ်"
```

**Output ရမှာ:**
- Complete auth flow
- Token management
- Security best practices
- Protected routes
- User session handling

---

### 🧪 Testing

**ဘယ်အချိန်သုံးမလဲ?**
- **Unit tests** ရေးချင်တဲ့အခါ
- **Integration tests** ရေးချင်တဲ့အခါ
- **End-to-end (E2E) tests** ရေးချင်တဲ့အခါ
- **Test coverage** တိုးချင်တဲ့အခါ
- **Existing code ကို test** ရေးချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "React component unit test ရေးမယ်"
✅ "API endpoint integration test ရေးမယ်"
✅ "Cypress E2E test ရေးမယ်"
✅ "Mock data နဲ့ test ရေးမယ်"
✅ "Edge cases အကုန် cover ဖြစ်အောင် test ရေးမယ်"
```

**Output ရမှာ:**
- Test file structure
- Test cases with descriptions
- Mock data setup
- Edge cases coverage
- Test running instructions

---

### 📦 Deployment

**ဘယ်အချိန်သုံးမလဲ?**
- **Production deploy** လုပ်ချင်တဲ့အခါ
- **CI/CD pipeline** setup လုပ်ချင်တဲ့အခါ
- **Docker containerization** လုပ်ချင်တဲ့အခါ
- **Cloud platform** (AWS, GCP, Vercel) deploy ချင်တဲ့အခါ
- **Environment configuration** လုပ်ချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "Docker နဲ့ containerize လုပ်မယ်"
✅ "GitHub Actions CI/CD pipeline ရေးမယ်"
✅ "AWS EC2 deploy လုပ်မယ်"
✅ "Vercel deployment setup လုပ်မယ်"
✅ "Kubernetes deployment ရေးမယ်"
```

**Output ရမှာ:**
- Dockerfile/docker-compose
- CI/CD configuration files
- Environment variables setup
- Deployment scripts
- Monitoring setup

---

### ⚡ Optimization

**ဘယ်အချိန်သုံးမလဲ?**
- App က **နှေးနေ**တဲ့အခါ
- **Memory usage** များနေတဲ့အခါ
- **Load time** လျှော့ချချင်တဲ့အခါ
- **Database queries** ပိုမြန်အောင် လုပ်ချင်တဲ့အခါ
- **Bundle size** လျှော့ချချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "React app performance optimize လုပ်မယ်"
✅ "SQL query ပိုမြန်အောင် optimize လုပ်မယ်"
✅ "Image lazy loading implement လုပ်မယ်"
✅ "Code splitting နဲ့ bundle size လျှော့မယ်"
✅ "Caching strategy implement လုပ်မယ်"
```

**Output ရမှာ:**
- Performance analysis
- Optimization techniques
- Before/After metrics
- Implementation code
- Monitoring recommendations

---

### 🏗️ Architecture

**ဘယ်အချိန်သုံးမလဲ?**
- **New project** စမယ့်အခါ structure ချချင်တဲ့အခါ
- **System design** လုပ်ချင်တဲ့အခါ
- **Microservices vs Monolith** ဆုံးဖြတ်ချင်တဲ့အခါ
- **Scalability planning** လုပ်ချင်တဲ့အခါ
- **Tech stack selection** လုပ်ချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "SaaS application architecture design လုပ်မယ်"
✅ "Microservices architecture design လုပ်မယ်"
✅ "Event-driven architecture implement လုပ်မယ်"
✅ "Monorepo structure setup လုပ်မယ်"
✅ "Scalable chat system design လုပ်မယ်"
```

**Output ရမှာ:**
- Architecture diagrams
- Component breakdown
- Technology recommendations
- Scalability considerations
- File/Folder structure

---

### 🎨 UI Component

**ဘယ်အချိန်သုံးမလဲ?**
- **Reusable UI component** ရေးချင်တဲ့အခါ
- **Design system** တည်ဆောက်ချင်တဲ့အခါ
- **Interactive UI elements** ရေးချင်တဲ့အခါ
- **Responsive design** implement လုပ်ချင်တဲ့အခါ
- **Animation/Transition** ထည့်ချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "Reusable Modal component ရေးမယ်"
✅ "Custom dropdown with search ရေးမယ်"
✅ "Data table with sorting, filtering ရေးမယ်"
✅ "Animated sidebar navigation ရေးမယ်"
✅ "Form with validation ရေးမယ်"
```

**Output ရမှာ:**
- Component code
- Props/API documentation
- Styling (CSS/Tailwind)
- Usage examples
- Accessibility considerations

---

### 🧮 Algorithm

**ဘယ်အချိန်သုံးမလဲ?**
- **Algorithm** ရေးချင်တဲ့အခါ
- **Data structure** implement လုပ်ချင်တဲ့အခါ
- **Coding interview prep** လုပ်ချင်တဲ့အခါ
- **Complex problem solving** လုပ်ချင်တဲ့အခါ
- **Time/Space complexity** optimize လုပ်ချင်တဲ့အခါ

**Example Scenarios:**
```
✅ "Binary search tree implement လုပ်မယ်"
✅ "Graph traversal algorithm ရေးမယ်"
✅ "Dynamic programming solution ရေးမယ်"
✅ "Sorting algorithm implement လုပ်မယ်"
✅ "LRU cache implement လုပ်မယ်"
```

**Output ရမှာ:**
- Algorithm implementation
- Time/Space complexity analysis
- Step-by-step explanation
- Visual representation (if applicable)
- Edge cases handling

---

### 🎯 Quick Decision Guide

**"ဒါကို ဘာရွေးရမလဲ?" - Quick Reference**

| သင် လုပ်ချင်တာ | ရွေးရမယ့် Use Case |
|---------------|-------------------|
| App feature အသစ်ထည့်မယ် | 🚀 Feature တည်ဆောက် |
| Error ဖြေရှင်းမယ် | 🐛 Debug / Fix |
| Code ပြင်ဆင်မယ် (feature အသစ်မထည့်ဘဲ) | ♻️ Refactor |
| Third-party service သုံးမယ် | 🔗 API Integration |
| Database ရေးမယ်/query ရေးမယ် | 🗄️ Database Design |
| Login system ရေးမယ် | 🔐 Authentication |
| Test ရေးမယ် | 🧪 Testing |
| Server တင်မယ်/DevOps | 📦 Deployment |
| App ပိုမြန်အောင် လုပ်မယ် | ⚡ Optimization |
| Project structure ချမယ် | 🏗️ Architecture |
| Button/Modal/Form ရေးမယ် | 🎨 UI Component |
| Data structure/Logic ရေးမယ် | 🧮 Algorithm |

---

### 💡 Pro Tips

**1. Complexity Level ရွေးနည်း**
- **Beginner**: Code သစ်စလေ့လာသူများအတွက်
- **Intermediate**: အခြေခံသိပြီးသူများအတွက်
- **Advanced**: Production-level code လိုချင်သူများအတွက်
- **Expert**: Complex systems, edge cases အကုန်ပါစေချင်သူများအတွက်

**2. Output Style ရွေးနည်း**
- **Step-by-Step**: လေ့လာချင်ရင် ဒါရွေး
- **Full Code**: Copy-paste လုပ်ပြီး သုံးချင်ရင် ဒါရွေး
- **Explain + Code**: နားလည်ချင်တယ် + code လည်းလိုချင်ရင် ဒါရွေး
- **Best Practices**: Industry standard လိုချင်ရင် ဒါရွေး
- **Compare**: Multiple solutions ကြည့်ချင်ရင် ဒါရွေး
- **Code Review**: Code quality ပိုကောင်းအောင် လုပ်ချင်ရင် ဒါရွေး

**3. Language တစ်ခုထက်မက ရွေးလို့ရတယ်**
Full-stack project ဆိုရင် React + Node.js + SQL စသည်ဖြင့် တစ်ခုထက်မက ရွေးနိုင်ပါတယ်။

---

## 1. Project စတင်ခြင်း

### Prompt:
```
AI Prompt Generator website တစ်ခု လုပ်ပေးပါ။
- Dark theme, modern UI
- Myanmar language support ပါရမယ်
- Category တွေ ရွေးလို့ရရမယ် (Coding, Writing, Marketing, Education, Business, Creative, Data, General)
- Tone ရွေးလို့ရရမယ် (Professional, Casual, Technical, Creative, Persuasive, Educational)
- Topic နဲ့ Context ထည့်ပြီး Prompt generate လုပ်လို့ရရမယ်
- Starry background animation ပါရမယ်
```

> **ရလဒ်**: Basic project structure, dark theme, starry background animation, PromptGenerator component

---

## 2. Authentication System

### Prompt:
```
User authentication system ထည့်ပေးပါ။
- Email နဲ့ signup/login လုပ်လို့ရရမယ်
- Password reset functionality ပါရမယ်
- Protected routes - login မလုပ်ရသေးရင် redirect လုပ်ပေးရမယ်
- User profile system ထည့်ပေးပါ
```

### Follow-up Prompt:
```
Change password feature ထည့်ပေးပါ - logged in user က password ပြောင်းလို့ရအောင်
```

> **ရလဒ်**: Auth pages, ProtectedRoute component, useAuth hook, ChangePasswordModal, ForgotPasswordModal

---

## 3. Prompt Generator Core Feature

### Prompt:
```
Gemini AI API သုံးပြီး prompt generate လုပ်ပေးတဲ့ edge function ရေးပေးပါ။
- User ရဲ့ topic, category, tone, context ကို ယူပြီး professional prompt တစ်ခု generate လုပ်ပေးရမယ်
- Myanmar language နဲ့ English နှစ်မျိုးလုံး support လုပ်ရမယ်
```

### Follow-up Prompt:
```
Generate ပြီးတဲ့ prompt ကို copy button နှိပ်ပြီး clipboard ကို copy ကူးလို့ရအောင် လုပ်ပေးပါ
```

> **ရလဒ်**: generate-prompt edge function, PromptOutput component with copy functionality

---

## 4. Coding Prompt Tab

### Prompt:
```
Coding Prompt tab တစ်ခု သီးသန့်ထည့်ပေးပါ။
- Programming language ရွေးလို့ရရမယ် (Python, JavaScript, TypeScript, React, etc.)
- Framework ရွေးလို့ရရမယ်
- Code type ရွေးလို့ရရမယ် (Function, Class, API, Full Project, etc.)
- Coding-specific prompt generate လုပ်ပေးရမယ်
```

> **ရလဒ်**: CodingPromptTab component with language/framework/type selectors

---

## 5. Image Prompt Tab

### Prompt:
```
AI Image generation အတွက် prompt ရေးပေးတဲ့ tab ထည့်ပေးပါ။
- Style ရွေးလို့ရရမယ် (Realistic, Anime, Digital Art, Oil Painting, etc.)
- Size ရွေးလို့ရရမယ်
- Image description ရေးပြီး optimized prompt ထုတ်ပေးရမယ်
- Midjourney, DALL-E, Stable Diffusion တွေအတွက် prompt format ထုတ်ပေးရမယ်
```

> **ရလဒ်**: ImagePromptTab, StyleSelector, SizeSelector components

---

## 6. Video Prompt Tab

### Prompt:
```
AI Video generation tools (Runway, Pika, Sora) တွေအတွက် prompt ရေးပေးတဲ့ tab ထည့်ပေးပါ။
- Video style ရွေးလို့ရရမယ်
- Duration, camera movement, mood ရွေးလို့ရရမယ်
- Myanmar dialogue support ပါရမယ်
```

> **ရလဒ်**: VideoPromptTab component with video-specific options

---

## 7. Image Generator Tab

### Prompt:
```
AI Image ကို တိုက်ရိုက် generate လုပ်ပေးတဲ့ tab ထည့်ပေးပါ။
- Description ရေးပြီး image generate လုပ်ပေးရမယ်
- Generated image ကို download လုပ်လို့ရရမယ်
- Edge function နဲ့ image generation API ချိတ်ဆက်ပေးပါ
```

> **ရလဒ်**: ImageGeneratorTab component, generate-image edge function

---

## 8. Ad Poster Generator Tab

### Prompt:
```
Advertising poster/banner အတွက် prompt generate လုပ်ပေးတဲ့ tab ထည့်ပေးပါ။
- Product/Service name ထည့်ရမယ်
- Target audience ရွေးလို့ရရမယ်
- Poster style ရွေးလို့ရရမယ်
- Marketing-focused prompt ထုတ်ပေးရမယ်
```

> **ရလဒ်**: AdPosterTab component

---

## 9. Image to Prompt Tab

### Prompt:
```
Image upload လုပ်ပြီး AI ကနေ prompt ပြန်ထုတ်ပေးတဲ့ tab ထည့်ပေးပါ။
- Image drag & drop / file upload ရရမယ်
- AI က image ကို analyze လုပ်ပြီး prompt ပြန်ရေးပေးရမယ်
- Prompt ကို copy ကူးလို့ရရမယ်
```

> **ရလဒ်**: ImageToPromptTab component, image-to-prompt edge function

---

## 10. Execute (AI Answer) Feature

### Prompt:
```
Prompt generate လုပ်ပြီးတဲ့အခါ "Generate + Execute" button ထည့်ပေးပါ။
- Prompt ကို generate လုပ်ပြီး AI ကနေ တိုက်ရိုက် result/answer ထုတ်ပေးရမယ်
- "Prompt Generate မယ်" button က prompt ပဲ ထုတ်ပေးမယ်
- "Generate + Execute" button က prompt generate ပြီး AI answer ပါ ထုတ်ပေးမယ်
- Button ၂ ခု ရဲ့ ကွာခြားချက်ကို ရှင်းပြတဲ့ description ထည့်ပေးပါ
```

> **ရလဒ်**: execute-prompt edge function, dual button UI, executed result display

---

## 11. UI/UX Improvements

### Prompt:
```
Tab navigation system ထည့်ပေးပါ - tab တွေအကုန်လုံးကို တစ်နေရာတည်းမှာ switch လုပ်လို့ရအောင်
```

### Follow-up Prompts:
```
Glass morphism effect ထည့်ပေးပါ
```

```
Light mode ပြောင်းလိုက်ရင် စာတွေ မှန်သွားတယ် - light mode မှာ contrast ပိုကောင်းအောင် fix ပေးပါ
```

```
Starry background animation ကို ပိုလှအောင် improve လုပ်ပေးပါ
```

> **ရလဒ်**: TabNav component, glass effects, theme improvements, StarryBackground component

---

## 12. How To Use Guide

### Prompt:
```
Feature တွေ အသုံးပြုနည်း ရှင်းပြတဲ့ "အသုံးပြုနည်း" tab ထည့်ပေးပါ။
- Tab တစ်ခုချင်းစီရဲ့ အသုံးပြုပုံကို ရှင်းပြပေးရမယ်
- Myanmar language နဲ့ ရေးပေးပါ
- Step by step guide ဖြစ်ရမယ်
```

> **ရလဒ်**: HowToUseTab component with comprehensive guide

---

## 13. Admin Panel

### Prompt:
```
Admin panel ထည့်ပေးပါ။
- Admin role ရှိတဲ့ user ပဲ access ရရမယ်
- Usage logs ကြည့်လို့ရရမယ်
- User management လုပ်လို့ရရမယ်
```

> **ရလဒ်**: Admin page, user_roles table, is_admin function, usage_logs table

---

## 14. SEO & Branding

### Prompt:
```
Browser Tab မှာ "ZTH Coder" လို့ နာမည် ပြောင်းပေးပါ
```

```
SEO meta tags တွေ ထည့်ပေးပါ - title, description, og tags
```

> **ရလဒ်**: Updated index.html with proper meta tags and branding

---

## 🎯 Prompt ရေးနည်း Tips

### ✅ ကောင်းတဲ့ Prompt ရေးနည်း:
1. **ရှင်းရှင်းလင်းလင်း** ဘာလိုချင်တယ်ဆိုတာ တိတိကျကျ ပြောပါ
2. **Feature list** ပါအောင် ရေးပါ (bullet points သုံးပါ)
3. **UI/UX details** ထည့်ပါ (e.g., "dark theme", "glass effect", "rounded corners")
4. **Language** ဘာသာစကား specify လုပ်ပါ
5. **တစ်ခုချင်းစီ** ခွဲပြီး request လုပ်ပါ - တစ်ခါတည်း အများကြီး မတောင်းပါနဲ့

### ❌ ရှောင်ရမယ့်အရာ:
1. တစ်ခါတည်း feature ၁၀ ခု မတောင်းပါနဲ့
2. မရှင်းတဲ့ instruction မပေးပါနဲ့
3. "ကောင်းကောင်းလုပ်ပေး" လို့ပဲ မပြောပါနဲ့ - specific ဖြစ်ပါစေ

### 💡 Pro Tips:
- Error ဖြစ်ရင် error message ကို copy paste လုပ်ပြီး "ဒီ error fix ပေးပါ" လို့ ပြောပါ
- Screenshot ပေးလို့ရရင် ပေးပါ - AI က image ကြည့်ပြီး fix ပေးနိုင်ပါတယ်
- "ဒီ feature ကို ဒီ style အတိုင်း လုပ်ပေးပါ" ဆိုပြီး reference ပေးပါ

---

## 15. 🔗 API Integration & Edge Functions

> ဒီ section မှာ AI API တွေနဲ့ ဘယ်လို ချိတ်ဆက်ခဲ့တယ်ဆိုတာကို အသေးစိတ် ရှင်းပြထားပါတယ်။

### 📌 API Architecture Overview

ဒီ project မှာ **Dual-Mode API System** သုံးထားပါတယ်:
1. **User ကိုယ်ပိုင် API Key** (Gemini / OpenRouter) — ဦးစားပေး အသုံးပြုမယ်
2. **Lovable AI Gateway** — Fallback (user key မရှိရင် / error ဖြစ်ရင်)

```
User Request → Edge Function → User API Key ရှိလား? 
                                    ├── ရှိရင် → User Key နဲ့ ကြိုးစား
                                    │            ├── အဆင်ပြေ → Result ပြန်
                                    │            └── Error (401/403) → Lovable AI Fallback
                                    └── မရှိရင် → Lovable AI Gateway
```

---

### 🔑 User API Key Setup Guide

> ဒီ project မှာ user တွေက ကိုယ်ပိုင် API key ထည့်ပြီး AI feature တွေကို သုံးနိုင်ပါတယ်။

#### 📋 Supported API Providers

| Provider | API Key Format | ဘာအတွက်သုံးလဲ | Free Tier |
|----------|---------------|---------------|-----------|
| **Gemini** | `AIza...` | Prompt generate, Execute, Image-to-Prompt | ✅ ရှိတယ် |
| **OpenRouter** | `sk-or-...` | Multiple AI models access | ❌ Credit လိုတယ် |

---

#### 🛠️ API Key ရယူနည်း

**Gemini API Key ရယူနည်း:**
1. [Google AI Studio](https://aistudio.google.com/) သို့သွားပါ
2. Google Account နဲ့ Sign In လုပ်ပါ
3. "Get API Key" ကို Click ပါ
4. "Create API Key" နှိပ်ပါ
5. Key ကို Copy ယူပါ (AIza... နဲ့ စတယ်)

**OpenRouter API Key ရယူနည်း:**
1. [OpenRouter.ai](https://openrouter.ai/) သို့သွားပါ
2. Account ဖန်တီးပြီး Sign In လုပ်ပါ
3. Settings > API Keys သို့သွားပါ
4. "Create Key" နှိပ်ပါ
5. Key ကို Copy ယူပါ (sk-or-... နဲ့ စတယ်)

---

#### 🖥️ App ထဲမှာ API Key ထည့်နည်း

1. App ရဲ့ Header မှာ **⚙️ Settings icon** ကို Click ပါ
2. **"API Key"** button ကို Click ပါ
3. Provider ရွေးပါ (Gemini သို့မဟုတ် OpenRouter)
4. API Key ထည့်ပါ
5. **"Save API Key"** နှိပ်ပါ

```
UI Flow:
Header Settings Icon (⚙️) → API Key Modal → Provider Select → Input Key → Save
```

---

#### 🔒 API Key Security Features

ဒီ project မှာ API Key တွေကို အောက်ပါ security measures တွေနဲ့ ကာကွယ်ထားပါတယ်:

| Feature | Description |
|---------|-------------|
| **AES-256-GCM Encryption** | Industry-standard encryption algorithm |
| **PBKDF2 Key Derivation** | 100,000 iterations နဲ့ key derive လုပ်ထား |
| **Masked Display** | UI မှာ `sk-A●●●●xyz1` format နဲ့ပဲ ပြတယ် |
| **Server-side Only** | Key ကို edge function ထဲမှာပဲ decrypt လုပ်တယ် |
| **User-specific** | User တစ်ယောက်ချင်းစီရဲ့ key ကို သီးသန့်သိမ်းထားတယ် |

---

#### 📊 API Key Database Schema

```sql
-- user_api_keys table structure
CREATE TABLE public.user_api_keys (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL,           -- ဘယ် user ရဲ့ key လဲ
  provider TEXT NOT NULL,           -- 'gemini' or 'openrouter'
  api_key TEXT NOT NULL,            -- Encrypted key (enc:v1:base64...)
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- RLS Policy: User ကိုယ်တိုင်ရဲ့ keys ပဲ access ရ
CREATE POLICY "Users can manage own keys"
  ON user_api_keys FOR ALL
  USING (auth.uid() = user_id);
```

---

#### 🔄 API Key Management Actions

`manage-api-key` edge function က အောက်ပါ actions တွေကို handle လုပ်ပါတယ်:

| Action | Description | Request Body |
|--------|-------------|--------------|
| `save` | Key အသစ်သိမ်း/update | `{ action: "save", provider: "gemini", apiKey: "AIza..." }` |
| `check` | Key ရှိမရှိ စစ်ဆေး | `{ action: "check", provider: "gemini" }` |
| `get` | Key ကို masked format နဲ့ ပြန်ယူ | `{ action: "get", provider: "gemini" }` |
| `delete` | Key ကို ဖျက်မယ် | `{ action: "delete", provider: "gemini" }` |

**Frontend Usage Example:**
```typescript
// Save API Key
const { data, error } = await supabase.functions.invoke("manage-api-key", {
  body: {
    action: "save",
    provider: "gemini",  // or "openrouter"
    apiKey: "AIzaSy..."
  }
});

// Check if key exists
const { data } = await supabase.functions.invoke("manage-api-key", {
  body: { action: "check", provider: "gemini" }
});
// data.exists → true/false

// Get masked key
const { data } = await supabase.functions.invoke("manage-api-key", {
  body: { action: "get", provider: "gemini" }
});
// data.maskedKey → "AIza●●●●●●xy1"

// Delete key
await supabase.functions.invoke("manage-api-key", {
  body: { action: "delete", provider: "gemini" }
});
```

---

### 🔐 API Key Encryption System (Technical Details)

#### Prompt:
```
User ရဲ့ API key ကို secure ဖြစ်အောင် AES-256-GCM encryption နဲ့ encrypt လုပ်ပြီး database မှာ သိမ်းပေးပါ။
- Save, Check, Get, Delete actions ပါရမယ်
- Key ကို masked format နဲ့ ပြပေးရမယ် (e.g., sk-A●●●●xyz1)
- Gemini နဲ့ OpenRouter provider ၂ ခုလုံး support လုပ်ရမယ်
- manage-api-key edge function တစ်ခု ဖန်တီးပေးပါ
```

#### ဘယ်လို အလုပ်လုပ်လဲ:
```
API Key Save Flow:
User Input "sk-abc123..." 
  → Edge Function (manage-api-key)
  → PBKDF2 Key Derivation (100,000 iterations)
  → AES-256-GCM Encryption
  → "enc:v1:base64data..." format နဲ့ Database မှာ သိမ်း
```

#### Encryption Code (Edge Function):
```typescript
const ENCRYPTION_PREFIX = "enc:v1:";

async function deriveKey(): Promise<CryptoKey> {
  const secret = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
  const encoder = new TextEncoder();
  const keyMaterial = await crypto.subtle.importKey(
    "raw", 
    encoder.encode(secret), 
    "PBKDF2", 
    false, 
    ["deriveKey"]
  );
  return crypto.subtle.deriveKey(
    { 
      name: "PBKDF2", 
      salt: encoder.encode("lovable-api-key-encryption-salt"), 
      iterations: 100000, 
      hash: "SHA-256" 
    },
    keyMaterial,
    { name: "AES-GCM", length: 256 },
    false,
    ["encrypt", "decrypt"]
  );
}

async function encrypt(text: string): Promise<string> {
  const key = await deriveKey();
  const iv = crypto.getRandomValues(new Uint8Array(12));
  const encoded = new TextEncoder().encode(text);
  const encrypted = await crypto.subtle.encrypt(
    { name: "AES-GCM", iv }, 
    key, 
    encoded
  );
  const combined = new Uint8Array(iv.length + encrypted.byteLength);
  combined.set(iv);
  combined.set(new Uint8Array(encrypted), iv.length);
  return ENCRYPTION_PREFIX + btoa(String.fromCharCode(...combined));
}

async function decrypt(encryptedText: string): Promise<string> {
  if (!encryptedText.startsWith(ENCRYPTION_PREFIX)) return encryptedText;
  const base64Data = encryptedText.slice(ENCRYPTION_PREFIX.length);
  const key = await deriveKey();
  const combined = Uint8Array.from(atob(base64Data), c => c.charCodeAt(0));
  const iv = combined.slice(0, 12);
  const data = combined.slice(12);
  const decrypted = await crypto.subtle.decrypt(
    { name: "AES-GCM", iv }, 
    key, 
    data
  );
  return new TextDecoder().decode(decrypted);
}
```

> **ရလဒ်**: `manage-api-key` edge function — save/check/get/delete actions, AES-256-GCM encryption

---

### 🔄 Multi-Provider Fallback System

#### ဘယ်လို အလုပ်လုပ်လဲ:

Edge functions တွေက user ရဲ့ API key ကို ဦးစားပေးသုံးပြီး error ဖြစ်ရင် fallback chain အတိုင်း ဆက်ကြိုးစားပါတယ်:

```typescript
interface UserApiConfig {
  apiUrl: string;
  apiKey: string;
  source: "openrouter" | "gemini" | "lovable";
}

async function getUserApiConfig(authHeader: string): Promise<UserApiConfig[]> {
  const configs: UserApiConfig[] = [];

  // 1st Priority: User's OpenRouter key
  const orKey = await getUserKey("openrouter");
  if (orKey) {
    configs.push({ 
      apiUrl: "https://openrouter.ai/api/v1/chat/completions",
      apiKey: await decrypt(orKey),
      source: "openrouter" 
    });
  }

  // 2nd Priority: User's Gemini key
  const geminiKey = await getUserKey("gemini");
  if (geminiKey) {
    configs.push({ 
      apiUrl: "https://generativelanguage.googleapis.com/v1beta/openai/chat/completions",
      apiKey: await decrypt(geminiKey),
      source: "gemini" 
    });
  }

  // 3rd Priority (Fallback): Lovable AI Gateway
  const LOVABLE_API_KEY = Deno.env.get("LOVABLE_API_KEY");
  if (LOVABLE_API_KEY) {
    configs.push({ 
      apiUrl: "https://ai.gateway.lovable.dev/v1/chat/completions",
      apiKey: LOVABLE_API_KEY,
      source: "lovable" 
    });
  }

  return configs;
}
```

#### Fallback Logic:

```typescript
const FALLBACK_STATUS_CODES = [400, 401, 403];

async function callAIWithFallback(
  configs: UserApiConfig[],
  buildRequest: (config: UserApiConfig) => { url: string; init: RequestInit }
): Promise<{ response: Response; source: string }> {
  
  for (let i = 0; i < configs.length; i++) {
    const config = configs[i];
    const { url, init } = buildRequest(config);
    
    console.log(`Trying AI call via ${config.source}...`);
    const response = await fetch(url, init);

    if (response.ok) {
      return { response, source: config.source };
    }

    // Auth error → try next provider
    if (FALLBACK_STATUS_CODES.includes(response.status) && i < configs.length - 1) {
      console.warn(`${config.source} failed [${response.status}], falling back...`);
      continue;
    }

    // Other errors → return error
    return { response, source: config.source };
  }
  
  throw new Error("No AI providers available");
}
```

#### Error Handling:

| Status Code | Meaning | Action |
|-------------|---------|--------|
| `400` | Bad Request | Fallback to next provider |
| `401` | Unauthorized (Invalid key) | Fallback to next provider |
| `403` | Forbidden | Fallback to next provider |
| `429` | Rate Limit | Show "ခဏစောင့်ပြီး ပြန်ကြိုးစားပါ" |
| `402` | Credits exhausted | Show "Credits ကုန်သွားပါပြီ" |

---

### 🤖 Prompt Generation API (generate-prompt)

#### Prompt:
```
Gemini AI API သုံးပြီး prompt generate လုပ်ပေးတဲ့ edge function ရေးပေးပါ။
- User ကိုယ်ပိုင် API key ရှိရင် အဲ့ key ကို ဦးစားပေး သုံးရမယ်
- User key error ဖြစ်ရင် (401, 403) Lovable AI Gateway ကို auto fallback လုပ်ရမယ်
- OpenRouter API format နဲ့လည်း compatible ဖြစ်ရမယ်
- Rate limit (429) နဲ့ Credit error (402) ကို handle လုပ်ပြီး Myanmar message ပြပေးရမယ်
- Topic, category, tone, context ကို ယူပြီး system prompt နဲ့ professional prompt generate လုပ်ပေးရမယ်
```

#### Edge Function Flow:
```
Frontend (supabase.functions.invoke("generate-prompt"))
  → Edge Function receives: { topic, category, tone, context }
  → getUserApiConfig() — User key ရှာ + Lovable AI fallback ထည့်
  → callAIWithFallback() — Provider chain try
  → AI Model: google/gemini-2.5-flash
  → System Prompt: "You are an expert AI prompt engineer..."
  → Response: { prompt: "generated text" }
```

#### Frontend Call:
```typescript
const { data, error } = await supabase.functions.invoke("generate-prompt", {
  body: {
    topic: "React dashboard component",
    category: "coding",
    categoryDescription: "Programming & Development",
    tone: "Technical",
    context: "Include dark mode support",
  },
});
// data.prompt → Generated prompt text
```

> **ရလဒ်**: `generate-prompt` edge function — multi-provider fallback, encrypted key decryption

---

### ⚡ Prompt Execution API (execute-prompt)

#### Prompt:
```
Generate ပြီးသား prompt ကို AI ကနေ တိုက်ရိုက် execute လုပ်ပြီး result ထုတ်ပေးတဲ့ edge function ရေးပေးပါ။
- generate-prompt function နဲ့ အတူတူ dual-mode API system သုံးရမယ်
- Prompt ကို AI ဆီ ပို့ပြီး comprehensive response ပြန်ရရမယ်
- max_tokens 4096 ထားပေးပါ (longer responses အတွက်)
- Temperature 0.7 ထားပေးပါ (balanced output)
```

#### Flow:
```
"Generate + Execute" Button Click
  → Step 1: generate-prompt edge function call → Prompt ရ
  → Step 2: execute-prompt edge function call → AI Answer ရ
  → UI: Prompt + Executed Result နှစ်ခုလုံး ပြ
```

#### Frontend Call:
```typescript
// Step 1: Generate prompt
const { data: promptData } = await supabase.functions.invoke("generate-prompt", {
  body: { topic, category, tone, context },
});

// Step 2: Execute the generated prompt
const { data: executeData } = await supabase.functions.invoke("execute-prompt", {
  body: { prompt: promptData.prompt },
});
// executeData.result → AI's comprehensive answer
```

> **ရလဒ်**: `execute-prompt` edge function — prompt ကို AI ကနေ တိုက်ရိုက် အဖြေထုတ်ပေး

---

### 🖼️ Image Generation API (generate-image)

#### Prompt:
```
AI Image generation edge function ရေးပေးပါ။
- Gemini image model (gemini-2.5-flash-image) သုံးပြီး image generate လုပ်ရမယ်
- modalities: ["image", "text"] ထည့်ပေးရမယ်
- Content safety filter (IMAGE_SAFETY) handle လုပ်ပေးရမယ်
- Model ကနေ image generate မပေးနိုင်ရင် refusal message ပြပေးရမယ်
- Generated image ကို base64 data URL format နဲ့ ပြန်ပေးရမယ်
```

#### Edge Function Key Config:
```typescript
// Image generation uses special model and modalities
body: JSON.stringify({
  model: "google/gemini-2.5-flash-image",  // Image-capable model
  messages: [{ role: "user", content: prompt }],
  modalities: ["image", "text"],  // ← ဒါက image generate ဖို့ အရေးကြီးတယ်
}),
```

#### Response Handling:
```typescript
const imageUrl = message?.images?.[0]?.image_url?.url;  // Base64 image data
const text = message?.content;  // Model's text response

// Safety filter check
if (finishReason === "IMAGE_SAFETY") {
  // Content safety ကြောင့် generate မပေးနိုင်ကြောင်း ပြ
}
```

> **ရလဒ်**: `generate-image` edge function — AI image generation with safety handling

---

### 🔄 Image-to-Prompt API (image-to-prompt)

#### Prompt:
```
Image URL ပေးလိုက်ရင် AI ကနေ analyze လုပ်ပြီး recreate လို့ရတဲ့ detailed prompt ပြန်ထုတ်ပေးတဲ့ edge function ရေးပေးပါ။
- Multimodal (image + text) input support ရှိရမယ်
- Image ရဲ့ subject, style, color, lighting, mood, composition အကုန် analyze လုပ်ရမယ်
- Midjourney/DALL-E/Stable Diffusion အတွက် သုံးလို့ရတဲ့ prompt format ထုတ်ပေးရမယ်
```

#### Multimodal Message Format:
```typescript
messages: [
  {
    role: "system",
    content: "You are an expert at analyzing images and creating detailed prompts..."
  },
  {
    role: "user",
    content: [
      { type: "text", text: "Analyze this image and create a detailed prompt..." },
      { type: "image_url", image_url: { url: imageUrl } },  // ← Image URL ထည့်
    ],
  },
],
```

> **ရလဒ်**: `image-to-prompt` edge function — multimodal image analysis

---

### 🔑 Fallback System (အဓိက Logic)

#### Prompt:
```
API call failure ဖြစ်ရင် automatic fallback system ထည့်ပေးပါ။
- User ရဲ့ ကိုယ်ပိုင် API key (OpenRouter/Gemini) ကို ဦးစားပေး သုံးရမယ်
- Authentication error (400, 401, 403) ဖြစ်ရင် Lovable AI Gateway ကို auto switch လုပ်ရမယ်
- Rate limit (429) နဲ့ Credit error (402) ကို Myanmar language နဲ့ error message ပြရမယ်
- Console log မှာ ဘယ် provider ကို သုံးတယ်ဆိုတာ ပြရမယ်
```

#### Fallback Chain Logic:
```typescript
// Provider priority chain
const configs: UserApiConfig[] = [];

// 1st Priority: User's OpenRouter key
if (openrouterKey) configs.push({ source: "openrouter", ... });

// 2nd Priority: User's Gemini key
if (geminiKey) configs.push({ source: "gemini", ... });

// 3rd Priority (Always): Lovable AI Gateway
configs.push({ source: "lovable", apiKey: LOVABLE_API_KEY, ... });

// Fallback logic
const FALLBACK_STATUS_CODES = [400, 401, 403];

for (const config of configs) {
  const response = await fetch(config.apiUrl, requestInit);
  if (response.ok) return { response, source: config.source };
  
  // Auth error → try next provider
  if (FALLBACK_STATUS_CODES.includes(response.status)) continue;
  
  // Other errors → return error
  return { response, source: config.source };
}
```

---

### 📊 Edge Functions Summary

| Function | Purpose | AI Model | Key Features |
|----------|---------|----------|-------------|
| `generate-prompt` | Prompt ထုတ်ပေး | gemini-2.5-flash | Category/Tone aware, multi-provider |
| `execute-prompt` | Prompt execute လုပ်ပေး | gemini-2.5-flash | 4096 max tokens, comprehensive answers |
| `generate-image` | Image generate လုပ်ပေး | gemini-2.5-flash-image | Multimodal output, safety filter |
| `image-to-prompt` | Image → Prompt | gemini-2.5-flash | Multimodal input, detailed analysis |
| `manage-api-key` | API key CRUD | - | AES-256-GCM encryption, multi-provider |

---

### 🛡️ Security Features

| Feature | Implementation |
|---------|---------------|
| API Key Encryption | AES-256-GCM with PBKDF2 key derivation |
| Key Storage | Encrypted in database, never plain text |
| Auth Check | JWT token validation per request |
| CORS Headers | Proper cross-origin configuration |
| Error Masking | Internal errors မပြ, user-friendly messages |
| Rate Limit Handling | 429/402 status codes with Myanmar messages |

---

### 💡 API ချိတ်ဆက်ရာတွင် Tips

1. **Edge Function ကို သုံးပါ** — Frontend ကနေ API ကို direct call မလုပ်ပါနဲ့
2. **API Key ကို encrypt လုပ်ပါ** — Plain text database မှာ မသိမ်းပါနဲ့
3. **Fallback system ထည့်ပါ** — User key fail ဖြစ်ရင် backup ရှိအောင်
4. **Error handling ကောင်းကောင်းလုပ်ပါ** — Rate limit, credit errors handle လုပ်ပါ
5. **CORS headers မမေ့ပါနဲ့** — Web app ကနေ call ရဖို့ CORS လိုပါတယ်
6. **Logging ထည့်ပါ** — Debug လုပ်ရတာ လွယ်အောင် console.log ထည့်ပါ



## 🛠️ Tech Stack

| Technology | Purpose |
|-----------|---------|
| React + TypeScript | Frontend Framework |
| Vite | Build Tool |
| Tailwind CSS | Styling |
| shadcn/ui | UI Components |
| Lovable Cloud (Supabase) | Backend, Auth, Database |
| Gemini AI | Prompt Generation & Execution |
| Edge Functions | Serverless Backend Logic |

---

## 📁 Project Structure

```
src/
├── components/
│   ├── PromptGeneratorTab.tsx    # Main prompt generator
│   ├── CodingPromptTab.tsx       # Coding-specific prompts
│   ├── ImagePromptTab.tsx        # Image prompt generator
│   ├── VideoPromptTab.tsx        # Video prompt generator
│   ├── ImageGeneratorTab.tsx     # AI image generation
│   ├── AdPosterTab.tsx           # Ad poster prompts
│   ├── ImageToPromptTab.tsx      # Image → Prompt conversion
│   ├── HowToUseTab.tsx           # Usage guide
│   ├── TabNav.tsx                # Tab navigation
│   ├── AppHeader.tsx             # App header with theme toggle
│   ├── PromptOutput.tsx          # Output display component
│   ├── StarryBackground.tsx      # Animated background
│   └── ProtectedRoute.tsx        # Auth guard
├── pages/
│   ├── Index.tsx                 # Main page
│   ├── Auth.tsx                  # Login/Signup
│   ├── Admin.tsx                 # Admin panel
│   └── ResetPassword.tsx         # Password reset
├── hooks/
│   ├── useAuth.tsx               # Authentication hook
│   └── useTheme.tsx              # Theme management
supabase/
└── functions/
    ├── generate-prompt/          # Prompt generation
    ├── execute-prompt/           # Prompt execution
    ├── generate-image/           # Image generation
    ├── image-to-prompt/          # Image analysis
    └── manage-api-key/           # API key management
```

---

## 📝 License

This project is built with [Lovable](https://lovable.dev).

---

> **Made with ❤️ by ZTH Coder**
